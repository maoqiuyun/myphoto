= link_to "Create a album", new_album_path
%ul.thumbnails
  - @albums.each do |album|
    %li.span2
      %a.thumbnail{href: album_path(album)}
        = image_tag album.photos.first.image.url(:thumb) rescue nil
      .caption
        %h5= album.name
        %p= album.description
