%div{style: "text-align: center" }= image_tag @photo.image
= @photo.name
= link_to "new", new_photo_path(album_id: @photo.album.id) rescue nil
= link_to "Edit", edit_photo_path(@photo)
= link_to "destory", photo_path(@photo), method: :delete, confirm: "Are you sure?"
